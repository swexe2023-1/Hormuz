<% @all_major.each do |e| %>
    
        <p>---------------------------------------</p>
        <p><%= e.jid %></p>
       
       <%= button_to 'この商品を検索する','serch_product/serch',{method: :get,params: {major_id: e.id}} %>
        <!--%= link_to '削除', user_path(e), data: { turbo_method: :delete } %-->
    
<% end %>

