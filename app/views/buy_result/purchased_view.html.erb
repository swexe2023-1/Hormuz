<h1>購入履歴</h1>

<% if @cart.size>=1 %>
<% @cart.each do |c| %>
<p>--------------------------</p>
<p>商品名:<%= Product.find(c).pid %></p>
<% if Product.find(c).img!=nil %>
<%= image_tag Product.find(c).img,:size =>'160x160' %>
<% end %>
<p>価格:<%= Product.find(c).price %> 円</p>
<p>個数:<%= @users_cart.where(product_id: c).count %></p>
<% end %>

<% else %>
<p>商品が購入されてません</p>
<% end %>